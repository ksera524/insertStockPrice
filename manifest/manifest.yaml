apiVersion: batch/v1
kind: CronJob
metadata:
  name: insertstockprice
  namespace: sandbox
spec:
  schedule: "*/4 * * * 2-6"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: insertstockprice
              image: qroksera.com/sandbox/insertstockprice:202403060519
              imagePullPolicy: IfNotPresent
              env:
                - name: TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: slack-secret
                      key: token
                - name: CHANNEL
                  valueFrom:
                    secretKeyRef:
                      name: slack-secret
                      key: channel
          imagePullSecrets:
            - name: harbor
          restartPolicy: OnFailure
